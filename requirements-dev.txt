# Dev / CI tooling — lint + tests + coverage gate.
# Install on top of the per-service deps (the shared `common` package carries pydantic/structlog):
#   pip install ./services/common \
#               -r services/api/requirements.txt \
#               -r services/detector/requirements.txt \
#               -r requirements-dev.txt
# Then run the gated suite:  pytest   (enforces >70% coverage, SPEC Part C)
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6.0
httpx>=0.27               # FastAPI TestClient transport — required by the tests/integration suites.
