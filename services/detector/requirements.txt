# Detector service dependencies.
opencv-python-headless>=4.9
numpy>=1.26
ultralytics>=8.2          # YOLO person detection (pulls torch, CPU)
confluent-kafka>=2.3      # publish detection.created events to Redpanda
